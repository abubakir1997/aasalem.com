import styled from 'styled-components'
import { Container } from '../elements/Container'

export const AASalemContent = styled(Container)`
  padding: 1rem 0;
  overflow: hidden;

  > * {
    justify-content: flex-start;
  }
`
